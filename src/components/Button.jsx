const Button = ({title}) => {
  return (
    <>
     <button className='font-abc text-base leading-5 py-[15px] px-[35px] border rounded-[40px] bg-white hover:bg-black hover:text-white'>{title}</button>
    </>
  )
}

export default Button